using UnityEngine;

public class Obj_TempatDrop : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Id_Drop Drop;
    public SpriteRenderer Gambar;
    
}